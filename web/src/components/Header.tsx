import Input from "./Input";

const Header: React.FC = () => {
    return (
        <div>
            <Input />
        </div>
    );
}

export const Foo = () => {
    return <div>Foo</div>
}

export default Header;
