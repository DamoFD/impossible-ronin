import { Input } from "@/components/ui/input";

const Header: React.FC = () => {
    return (
        <div>
            <Input />
        </div>
    );
}

export const Foo = () => {
    return <div>Foo</div>
}

export default Header;
